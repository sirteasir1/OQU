# OQU - Персонализированное Обучение Математике с ИИ

> Делаем математику увлекательной, связывая её с тем, что любят студенты

**OQU** — это образовательная платформа на базе искусственного интеллекта, которая генерирует персонализированные уроки и тесты по математике на основе интересов студентов. Создана для казахстанских школьников (7-11 классы) и превращает абстрактные математические концепции в понятный и увлекательный контент.

---

## 🌟 Что Делает OQU Уникальным

### 1. **Персонализация на Основе Интересов**
В отличие от традиционных образовательных платформ, OQU не просто адаптирует сложность — она **трансформирует контекст**:
- Фанат футбола? Изучай пропорции через статистику голов
- Любитель музыки? Понимай дроби через ритмические паттерны
- Энтузиаст технологий? Исследуй алгоритмы через примеры программирования

**Результат**: Математика становится актуальной для *их* мира, повышая вовлеченность и запоминаемость.

### 2. **Генерация Контента с Помощью ИИ**
Каждый урок и тест генерируется по запросу с использованием **Google Gemini AI**:
- **Уроки из 3 карточек**: Метафора → Теория → Пример
- **Тесты из 5 вопросов** с двойной обратной связью:
  - ✨ Почему правильные ответы верны
  - 🤔 Почему неправильные ответы ошибочны (сократический подход)

**Результат**: Бесконечный персонализированный контент с нулевой маржинальной стоимостью.

---

## 🚀 Основные Функции

- 📚 **67 Математических Тем** для 7-11 классов (программа Казахстана)
- 🎯 **38 Интересов** в 7 категориях (Музыка, Спорт, Игры, Фильмы, Еда, Технологии, Путешествия)
- 🤖 **Контент, Генерируемый ИИ** (Google Gemini)
- 📊 **Аналитика Обучения** (отслеживание прогресса, история, значки)
- ⚡ **Интеллектуальное Кэширование** (мгновенная загрузка после первого посещения)

---

## 🛠️ Технологический Стек

- **Фреймворк**: Next.js (App Router, React 19)
- **ИИ**: Google Gemini AI
- **Язык**: TypeScript (строгий режим)
- **Стилизация**: TailwindCSS 3.4
- **Хранилище**: localStorage + sessionStorage (клиентская сторона)
- **Аутентификация**: Supabase Auth (email/password)

---

## 📦 Локальная Установка

### Требования
- Node.js 18+ и npm
- API ключ Google Gemini (получить в Google AI Studio)
- Supabase URL / ANON KEY (создать проект в Supabase)

### Установка

1. **Клонируйте репозиторий**
   ```bash
   git clone <repository-url>
   cd oqu
   ```

2. **Установите зависимости**
   ```bash
   npm install
   ```

3. **Настройте переменные окружения**

   Создайте файл `.env.local` в корне проекта (можно начать с `.env.example`):
   ```bash
   # Gemini (server-side)
   GEMINI_API_KEY=ваш_api_ключ

   # Supabase
   NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_anon_key
   ```

   Затем в Supabase (SQL Editor) создайте таблицу `profiles` (нужна для хранения интересов):
   ```sql
   create table if not exists public.profiles (
     id uuid primary key references auth.users(id) on delete cascade,
     interests text[] not null default '{}'::text[],
     created_at timestamptz not null default now(),
     updated_at timestamptz not null default now()
   );

   create or replace function public.set_updated_at()
   returns trigger as $$
   begin
     new.updated_at = now();
     return new;
   end;
   $$ language plpgsql;

   drop trigger if exists set_profiles_updated_at on public.profiles;
   create trigger set_profiles_updated_at
   before update on public.profiles
   for each row execute function public.set_updated_at();

   alter table public.profiles enable row level security;

   create policy "Profiles are viewable by owner"
   on public.profiles for select
   using (auth.uid() = id);

   create policy "Profiles are insertable by owner"
   on public.profiles for insert
   with check (auth.uid() = id);

   create policy "Profiles are updatable by owner"
   on public.profiles for update
   using (auth.uid() = id)
   with check (auth.uid() = id);
   ```

4. **Запустите сервер разработки**
   ```bash
   npm run dev
   ```

5. **Откройте в браузере**
   ```
   http://localhost:3000
   ```

### Пользовательский Сценарий

1. **Выберите Интересы** - Выберите то, что вам нравится (Футбол, Музыка, Игры и т.д.)
2. **Выберите Класс** - Выберите уровень (7-11)
3. **Выберите Тему** - Просмотрите 67 математических тем
4. **Учитесь** - Читайте персонализированный урок, сгенерированный ИИ
5. **Проверьте Себя** - Пройдите тест из 5 вопросов
6. **Анализируйте** - Получите детальную обратную связь с объяснениями

---

**Создано с ❤️ используя технологии ИИ в образовании**
